import React from 'react';

// Explicitly typing the component as a React Functional Component (optional but helpful)
const App: React.FC = () => {
  return (
    <div>
      <h1>Hello Vite + TypeScript!</h1>
      <p>Your TSX component is now rendering correctly.</p>
    </div>
  );
};

export default App;
